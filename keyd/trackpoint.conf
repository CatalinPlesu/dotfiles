# /etc/keyd/trackpoint.conf

[ids]
# Only target the TPPS/2 Elan TrackPoint device
0fac:1ade
0002:000a

[main]
# Remap the TrackPoint buttons (which keyd sees as mouse buttons)
leftmouse = leftshift
rightmouse = leftcontrol
middlemouse = escape
