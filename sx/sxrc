#!/bin/sh
. $XDG_CONFIG_HOME/x11/xprofile 

case $1 in
    bspwm             ) exec bspwm;;
    openbox           ) exec openbox;;
    dwm				  ) exec dwm;;
	qtile			  ) exec qtile start;;
    *                 ) exec dwm;;

esac
